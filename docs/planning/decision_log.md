# 의사결정 기록

이 문서는 프로젝트를 만들면서 중요한 방향을 왜 선택했는지 기록한다. 나중에 README, 자소서, 면접 답변을 만들 때 근거로 사용한다.

## 2026-06-01 AI 답변 생성에서 데이터 제작으로 방향 전환

결정:

- AI가 정답을 완성하는 챗봇보다, 사람이 검수 가능한 학습 데이터 후보를 만드는 도구에 집중한다.

이유:

- 작은 로컬 LLM은 비용 없이 실험하기 좋지만 말투 유지, 사실 정확도, 긴 답변 안정성이 약했다.
- 모델이 모르는 내용을 그럴듯하게 만드는 할루시네이션 문제가 있었다.
- 파인튜닝에 쓸 데이터는 사람이 통제한 사실과 검수된 답변이어야 품질을 유지할 수 있다.

영향:

- 데이터 제작 탭을 추가했다.
- 질문, 핵심 사실, 원하는 말투를 따로 입력하는 흐름을 만들었다.
- AI는 초안 생성 보조 역할로 두고, 최종 답변은 사람이 수정해서 저장하는 방향으로 바뀌었다.

## 2026-06-02 문서 체계 분리

결정:

- `docs`를 요구사항, 데이터 모델, 테스트, 로그, 포트폴리오 방향성 문서로 나누어 관리한다.

이유:

- 하나의 상태 문서에 모든 내용을 넣으면 프로젝트가 커질수록 찾기 어려워진다.
- 포트폴리오에서 설명할 문제 정의, 구조, 한계, 개선 방향을 따로 관리할 필요가 있다.
- 기능을 추가할 때 문서도 함께 갱신하는 습관을 만들기 위해서다.

영향:

- `requirements`, `data`, `checklist`, `test_plan`, `implementation_log` 등으로 문서를 분리했다.
- 커밋 메시지 규칙과 문서 갱신 규칙을 만들었다.

## 2026-06-02 코드 구조는 바로 옮기지 않고 폴더부터 만든다

결정:

- 기존 JavaScript 파일을 바로 이동하지 않고, 먼저 `src` 폴더 구조만 만든다.

이유:

- 지금 파일 이동까지 같이 하면 import 경로 변경으로 앱이 깨질 수 있다.
- 아직 기능이 계속 바뀌고 있으므로, 먼저 어디에 무엇을 둘지 책임을 정하는 편이 안전하다.
- 다음 기능부터 새 구조에 맞춰 점진적으로 분리하는 것이 추적하기 쉽다.

영향:

- `src/core`, `src/features`, `src/ui`, `src/api`, `src/utils` 폴더를 만들었다.
- 기존 `app.js`, `chat.js`, `dataset.js` 등은 아직 유지한다.
- 다음 리팩터링은 데이터셋 검수 기능을 만들면서 `dataset` 영역부터 분리한다.

## 2026-06-02 프롬프트 참고 방식에서 LoRA 실험으로 전환

결정:

- 50개 좋은 예시를 프롬프트에 참고시키는 방식만으로는 완성된 나비를 만들기 어렵다고 판단한다.
- 다음 단계는 `ai-maker-dataset.jsonl` 50개를 사용한 작은 LoRA 학습 실험으로 진행한다.

이유:

- 현재 방식은 모델 자체를 학습시키는 파인튜닝이 아니라 프롬프트 참고 방식이다.
- 좋은 예시를 넣어도 작은 로컬 모델은 중국어를 섞거나, 냥체를 놓치거나, 예시 내용을 어색하게 따라 할 수 있다.
- 원하는 말투와 짧은 답변 패턴을 모델에 더 가깝게 반영하려면 LoRA 어댑터를 만들어 학습 전후를 비교해야 한다.

영향:

- 50개 큐레이션 데이터는 LoRA 1차 실험용 기준 데이터셋으로 본다.
- RTX 3050 8GB 환경에서는 처음부터 7B가 아니라 0.5B, 1.5B, 3B 순서로 실험한다.
- 포트폴리오에서는 "자동 생성 실패 → 기준 데이터셋 제작 → LoRA 실험" 흐름을 개선 과정으로 설명할 수 있다.

## 2026-06-02 LoRA 생성기 방향으로 확장

결정:

- AI Maker Studio를 단순 채팅/데이터 저장 도구에서 템플릿 기반 LoRA 데이터셋 생성 워크벤치로 확장한다.
- 사용자는 LoRA 프로젝트를 만들고, 목적별 템플릿과 체크리스트를 정의한다.
- 앱은 생성 AI, 수정 AI, 코드 검사기, 평가 AI, 사람 검수를 거쳐 데이터 후보를 만든다.

이유:

- ChatGPT나 Codex는 데이터 후보를 빠르게 만들 수 있지만, 일회성 대화만으로는 생성 기준, 탈락 이유, 검수 상태, 학습 결과를 추적하기 어렵다.
- LoRA 1차 실험으로 50개 JSONL에서 adapter 생성까지 확인했으므로, 다음 단계는 데이터를 더 체계적으로 만드는 흐름이다.
- 여러 목적의 LoRA를 만들려면 데이터셋, 템플릿, 체크리스트, 학습 결과를 프로젝트별로 분리해서 관리해야 한다.

영향:

- 기존 AI 프로필 중심 구조는 LoRA 프로젝트 중심 구조로 점진적으로 바꾼다.
- 데이터 제작 탭은 템플릿 기반 생성 흐름으로 확장한다.
- 데이터셋에는 프로젝트, 템플릿, 검수 상태, 평가 결과 같은 메타데이터가 필요해진다.
- 번역 LoRA, 말투 LoRA, 평가 LoRA, 수정 LoRA 같은 목적별 확장이 가능해진다.
